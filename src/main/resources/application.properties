spring.application.name=car_data_analysis

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/car_data_analysis}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

server.port=8080

# This file is public on GitHub - no default here. Set the JWT_SECRET env var
# (e.g. in your IntelliJ run configuration) or the app will fail to start.
jwt.secret=${JWT_SECRET}